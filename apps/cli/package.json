{
  "name": "@securepad/cli",
  "version": "workspace:*",
  "description": "Zero-knowledge encrypted notepad CLI",
  "type": "module",
  "bin": {
    "securepad": "./bin/securepad.js"
  },
  "scripts": {
    "dev": "echo 'CLI ready'",
    "build": "echo 'No build needed'"
  },
  "dependencies": {
    "@securepad/shared": "workspace:*",
    "commander": "^13.1.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
